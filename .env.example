# Copy to .env and fill in. Without a key, the system runs in offline mock mode.
ANTHROPIC_API_KEY=sk-ant-api03-REPLACE_WITH_YOUR_KEY
# Optional: point the dashboard at a deployed API (else it uses bundled demo data)
NEXT_PUBLIC_API_BASE=
